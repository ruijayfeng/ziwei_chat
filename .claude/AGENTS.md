# .claude/
> L2 | Parent: AGENTS.md

Member List
skills/: Claude Code skill symlinks pointing at `.agents/skills` so installed skills are available in this project.

Boundary
`.claude/skills` is project-local agent wiring. The canonical installed skill payload lives under `.agents/skills`; keep symlinks aligned with `skills-lock.json`.

[PROTOCOL]: Update this header when changed, then check AGENTS.md
